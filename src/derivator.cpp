#include "derivator.h"
#include <iostream>
#include <armadillo>


arma::mat create_mat_deriv(int n, int m){

    arma::mat A = arma::eye(n,m);
}

void derivator(arma::mat *M){
    arma::mat A = create_mat_derivee(size(M)); 
}
